import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    height: "100%",
  },
  content: {
    paddingHorizontal: 20,
    paddingBottom: 200,
  },
  containerSearch: {
    width: "100%",
    display: "flex",
    alignItems: "center",
    marginBottom: 20,
  },
});
