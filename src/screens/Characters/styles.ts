import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    height: "100%",
  },
  content: {
    paddingTop: 0,
    paddingBottom: 200,
    paddingHorizontal: 20,
  },
  containerSearch: {
    width: "100%",
    display: "flex",
    alignItems: "center",
    marginBottom: 20,
  },
});
