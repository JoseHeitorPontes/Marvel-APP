import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    height: "100%",
    paddingTop: 15,
  },
  containerSearch: {
    width: "100%",
    display: "flex",
    alignItems: "center",
    paddingHorizontal: 12.5,
  },
});
